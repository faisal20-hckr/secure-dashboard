diff --git a/templates/index.html b/templates/index.html
new file mode 100644
index 0000000..1111111
--- /dev/null
+++ b/templates/index.html
@@
+<!doctype html>
+<html lang="en">
+<head>
+  <meta charset="utf-8">
+  <title>SDP Secure Dashboard</title>
+  <link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
+</head>
+<body>
+  <header class="topbar">
+    <div class="brand">SDP Secure</div>
+    <nav>
+      {% if user %}
+      <a href="{{ url_for('dashboard') }}">Dashboard</a>
+      <a href="{{ url_for('logout') }}">Logout</a>
+      {% else %}
+      <a href="{{ url_for('login') }}">Login</a>
+      <a href="{{ url_for('register') }}">Register</a>
+      {% endif %}
+    </nav>
+  </header>
+  <main class="container">
+    <section class="hero">
+      <h1>Offline-ready 2FA Dashboard</h1>
+      <p>Monitor logins, devices and IP offline using Flask + SQLite + TOTP.</p>
+      {% if user %}
+      <a class="btn" href="{{ url_for('dashboard') }}">Open Dashboard</a>
+      {% else %}
+      <a class="btn" href="{{ url_for('register') }}">Get Started</a>
+      {% endif %}
+    </section>
+  </main>
+  <footer class="footer">Made for SDP Lab • Offline demo</footer>
+</body>
+</html>
diff --git a/templates/login.html b/templates/login.html
new file mode 100644
index 0000000..2222222
--- /dev/null
+++ b/templates/login.html
@@
+<!doctype html>
+<html lang="en">
+<head>
+  <meta charset="utf-8">
+  <title>Login / Register</title>
+  <link rel="stylesheet" href="{{ url_for('static', filename='css/style.css') }}">
+</head>
+<body>
+<header class="topbar"><div class="brand">SDP Secure</div></header>
+<main class="auth">
+  <div class="card">
+    <h2>{{ 'Register' if register else 'Login' }}</h2>
+    {% if error %}<p class="err">{{ error }}</p>{% endif %}
+    <form method="post">
+      <label>Username</label><input name="username" required>
+      <label>Password</label><input name="password" type="password" required>
+      <button class="btn" type="submit">{{ 'Register' if register else 'Login' }}</button>
+    </form>
+    <p class="muted">
+      {% if register %}
+      Already have account? <a href="{{ url_for('login') }}">Login</a>
+      {% else %}
+      New user? <a href="{{ url_for('register') }}">Register</a>
+      {% endif %}
+    </p>
+  </div>
+</main>
+<footer class="footer">SDP Lab</footer>
+</body>
+</html>
diff --git a/static/css/style.css b/static/css/style.css
new file mode 100644
index 0000000..3333333
--- /dev/null
+++ b/static/css/style.css
@@
+:root{--bg:#f5f7fb;--card:#ffffff;--muted:#6b7280;--accent:#2563eb;--danger:#ef4444;--radius:12px;--shadow:0 6px 18px rgba(16,24,40,0.08);font-family:Inter,ui-sans-serif,system-ui,-apple-system,"Segoe UI",Roboto,"Helvetica Neue",Arial}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:#0f172a}.topbar{height:64px;background:#0f172a;color:white;display:flex;align-items:center;justify-content:space-between;padding:0 20px}.topbar .brand{font-weight:700}.topbar nav a{color:#cbd5e1;margin-left:16px;text-decoration:none}.container{max-width:980px;margin:28px auto;padding:0 16px}.hero{background:linear-gradient(90deg, rgba(37,99,235,0.08), rgba(99,102,241,0.04));padding:36px;border-radius:12px;box-shadow:var(--shadow)}.btn{display:inline-block;background:var(--accent);color:white;padding:10px 14px;border-radius:10px;text-decoration:none}.card{background:var(--card);padding:20px;border-radius:12px;box-shadow:var(--shadow);margin-bottom:16px}.auth{display:flex;align-items:center;justify-content:center;height:calc(100vh - 120px)}.auth .card{width:380px}label{display:block;margin-top:8px;font-size:13px;color:var(--muted)}input{width:100%;padding:10px;margin-top:6px;border-radius:8px;border:1px solid #e6eef8;font-size:15px}.err{color:var(--danger)}.footer{padding:16px;text-align:center;color:var(--muted)}
